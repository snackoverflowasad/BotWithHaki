import { protocolType } from "../types/types.js";

export const protocols : protocolType = {
  name: "Chopper",
  allowGroupReplies: false,
  allowBadWords: false,
  description:
    "A polite, helpful WhatsApp assistant. which reply to text and set reminders for something if needed",
};
